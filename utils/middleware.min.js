import{NextResponse}from"next/server";export function middleware(request){return!(request.headers.get("referer")||"").includes(request.nextUrl.hostname)&&request.nextUrl.pathname.startsWith("/api/icon")?NextResponse.json({error:"Unauthorized"},{status:403}):NextResponse.next()}export const config={matcher:["/api/icon"]};